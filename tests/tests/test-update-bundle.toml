[[systems]]
system  = "customized-arm64"
ssh = { private-key = "./files/id_rsa" }

[[steps]]
action = "run"
description = "check system state prior to update"
script = """
#!/bin/bash
rugix-system-assert ".defaultBootGroup" "a"
rugix-system-assert ".activeBootGroup" "a"
"""

[[steps]]
action = "run"
description = "install a system update"
stdin-file = "./build/bundles/customized-arm64.rugixb"
# Rebooting may cause the SSH client to disconnect while executing the script.
may-disconnect = true
script = """
#!/bin/bash
rugix-ctrl update install -
"""

[[steps]]
action = "wait"
duration = 10

[[steps]]
action = "run"
description = "check whether the update has been installed"
script = """
#!/bin/bash
rugix-system-assert ".defaultBootGroup" "a"
rugix-system-assert ".activeBootGroup" "b"
rugix-ctrl system commit
rugix-system-assert ".defaultBootGroup" "b"
rugix-system-assert ".activeBootGroup" "b"
"""